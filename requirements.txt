# Core dependencies are declared in pyproject.toml.
# Environment and tooling are reproducible with uv sync --locked --extra embeddings.
# Web scraping now consumes indexed Markdown documentation.
# Embeddings and vector storage are installed through the package extra below.
# Data processing versions are resolved in uv.lock, not duplicated here.
.[embeddings]
